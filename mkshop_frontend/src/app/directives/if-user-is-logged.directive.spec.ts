import { IfUserIsLoggedDirective } from './if-user-is-logged.directive';

describe('IfUserIsLoggedDirective', () => {
  it('should create an instance', () => {
    const directive = new IfUserIsLoggedDirective();
    expect(directive).toBeTruthy();
  });
});
